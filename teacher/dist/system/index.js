'use strict';

System.register([], function (_export, _context) {
	"use strict";

	function configure(config) {
		config.globalResources(['./entity_teacher', './entity_teacher_edit']);
	}

	_export('configure', configure);

	return {
		setters: [],
		execute: function () {}
	};
});